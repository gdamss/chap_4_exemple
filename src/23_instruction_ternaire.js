const age = 15;

const status = age >= 18 ? "majeur" : age < 2 ? "baby" : "mineur";

console.log(status);

// if (age >= 18) {
//   isMajor = true;
// } else {
//   isMajor = false;
// }

// console.log(isMajor);
